<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.agentprojectjava.mapper.LoginMapper">
    <select id="LoginByPassword" resultType="org.example.agentprojectjava.pojo.NormalUser">
        select id,userEmail,userName,avatar from myuser
        <where>
            userName = #{userName} and userPassword = #{userPassword}
        </where>
    </select>
    <update id="LastLogin">
        update myuser
        set lastLogin = NOW()
        where id = #{id}
    </update>
    <select id="LoginByEmail" resultType="org.example.agentprojectjava.pojo.NormalUser">
        select id,userEmail,userName,avatar from myuser
        where userEmail = #{userEmail}
    </select>
</mapper>